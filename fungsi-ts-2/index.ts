//! tanpa return akan void
// let asep = (hobi: string | number) => {
//   console.log(`my hobi is ${hobi}`);
// };

// asep("ngoding");

// let umkm = (nama: string, cabang: number): void => {
//   console.log(`nama usaha saya ${nama} dan cabang ke ${cabang}`);
// };
// umkm("komo", 2);

// //! dengan return akan mengembalikan nilai
// let nama = (name: string, usia: number) => {
//   return `nama saya ${name} dan umur saya ${usia} tahun`;
// };

// let hasil = (hutang: number, uang: number) => {
//   return `hutang tersisa ${hutang - uang}`;
// };
// console.log("ini sisa uang saya,setelah bayar", hasil(1000, 5000));
// console.log(nama("kko", 22));

//? membuat function untuk pengembalian
// let pembagian = (kotak: number): boolean => {
//   return kotak % 2 === 0;
// };

// console.log(pembagian(21));
